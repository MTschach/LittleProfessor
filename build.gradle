allprojects {
   repositories {
		jcenter() 
   }
}



subprojects {
	apply plugin: 'java'
   version = '1.0'
}


task muleapp(type: Copy) {
    def jars=[]
    subprojects.each {
        jars+=it.libsDir
    }
    from jars
    into 'bin'
}

